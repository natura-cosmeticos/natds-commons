{{> fileHeader }}

protocol Components {
{{#each components}}
  var {{name}}: {{attributes.customOptions.type}} { get }
{{/each}}
}

protocol Tokens {
{{#each tokens}}
  var {{name}}: {{attributes.customOptions.type}} { get }
{{/each}}
}

protocol Theme {
  var tokens: Tokens
  var components: Components
}
