{{> fileHeader }}

protocol Theme {
  var tokens: Tokens
  var components: Components
}

protocol Tokens {
{{#each tokens}}
  var {{name}}: {{attributes.customOptions.type}} { get }
{{/each}}
}

protocol Components {
{{#each components}}
  var {{name}}: {{attributes.customOptions.type}} { get }
{{/each}}
}
