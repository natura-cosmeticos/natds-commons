{
  "name": "@naturacosmeticos/natds-themes",
  "version": "0.0.0",
  "description": "The single source of truth for Natura Desing System tokens.",
  "main": "./config/build.js",
  "scripts": {
    "test": "jest --collectCoverage",
    "lint": "eslint --ext .js ./",
    "build": "rm -rf ./build && babel-node --presets @babel/preset-env ./config/build.js && babel-node --presets @babel/preset-env ./config/postBuild.js && cp ./config/ios/static/* ./build/ios",
    "start": "http-server build/html"
  },
  "license": "ISC",
  "devDependencies": {}
}