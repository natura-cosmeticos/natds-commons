{{> fileHeader }}

struct Components: Components {
{{#each components}}
  let {{name}}{{#if attributes.customOptions.includeType}}: {{attributes.customOptions.type}}{{/if}} = {{value}}
{{/each}}
}

struct Tokens: Tokens {
{{#each tokens}}
  let {{name}}{{#if attributes.customOptions.includeType}}: {{attributes.customOptions.type}}{{/if}} = {{value}}
{{/each}}
}

struct Theme: Theme {
  let tokens: Tokens
  let components: Components
}
